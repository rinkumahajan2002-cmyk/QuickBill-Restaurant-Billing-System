# App name
spring.application.name=app

# Server config (IMPORTANT for Render)
server.port=${PORT:8085}
server.address=0.0.0.0

# Database (use env vars on Render, works locally if set)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/zomato_thymeleaf}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:Rinku@22}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Thymeleaf
spring.thymeleaf.cache=false

